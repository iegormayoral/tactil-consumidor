import { RippleDirective } from './ripple.directive';

describe('RippleDirective', () => {
  it('should create an instance', () => {
    const directive = new RippleDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
